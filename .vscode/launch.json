{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach",
            "type": "lldb",
            "request": "attach",
            "pid": "${command:pickMyProcess}"
        }
    ]
}
